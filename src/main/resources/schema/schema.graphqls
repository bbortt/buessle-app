type Query {
  boardByUUID(uuid: ID!): Board
}

type Mutation {
  createBoard(name: String!): Board!
  joinBoard(uuid: ID!): Board!
  registerPlayer(name: String!): Player!
}

type Board {
  uuid: ID!
  name: String!
  players: [Player]!
}

type Player {
  name: String!
}
