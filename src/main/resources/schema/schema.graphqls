type Query {
  boardByUUID(uuid: String!): Board
}

type Mutation {
  createBoard(name: String!): Board!
  registerPlayer(name: String!): Player!
}

type Board {
  uuid: ID!
  name: String!
}

type Player {
  name: String!
}
